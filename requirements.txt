# Pinned dependencies for reproducible Docker builds.
#
# The Reticulum stack (rns / lxmf / nomadnet) ships frequently with tight
# inter-package version constraints. The versions below are a known-good
# mutually-compatible set — verify with `pip install -r requirements.txt`
# in a fresh venv before bumping any of them.

# Reticulum stack
# Diagnostic revert (2026-06-05): rns 1.3.x introduces link-handshake
# behaviour that silently breaks inbound link establishment to sites
# hosted on NomadPortal (Symptom: "Timeout waiting for RTT packet from
# link initiator" / "No interfaces could process the outbound packet"
# in mirror logs; clients see the announce but can't navigate). Pinning
# to the most-recent pre-1.3 rns release to confirm whether 1.3.0
# introduced the regression. If 1.2.9 also fails, bisect further to
# 1.1.x. If 1.2.9 works, file an upstream issue against rns 1.3.0.
rns==1.2.9
lxmf==0.9.9
nomadnet==1.2.2

# Web app stack
flask==3.1.3
flask-login==0.6.3
authlib==1.7.2
requests==2.34.2
pyyaml==6.0.3
gunicorn==26.0.0
pytest==9.0.3

# Renderer — published to PyPI from v1.0.7 onwards
Micron2HTML==1.0.8
